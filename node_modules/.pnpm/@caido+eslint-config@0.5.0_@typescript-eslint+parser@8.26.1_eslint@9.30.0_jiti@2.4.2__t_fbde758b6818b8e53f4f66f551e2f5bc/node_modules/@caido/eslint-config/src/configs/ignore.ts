import { type FlatConfigItem } from "../types";

export const ignore = (): FlatConfigItem[] => {
  return [
    {
      name: "caido/ignore",
      ignores: ["**/coverage", "**/dist"],
    },
  ];
};
