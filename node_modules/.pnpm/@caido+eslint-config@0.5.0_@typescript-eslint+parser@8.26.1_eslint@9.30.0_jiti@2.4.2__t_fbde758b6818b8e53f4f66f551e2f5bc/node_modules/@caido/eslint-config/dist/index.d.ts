import { Linter } from 'eslint';

type FlatConfigItem = Linter.Config;
type ESLintConfigOptions = {
    javascript: boolean;
    typescript: boolean;
    vue: boolean;
    prettier: boolean;
    compat: boolean;
    imports: boolean;
    node: boolean;
};

declare const defaultConfig: (options?: Partial<ESLintConfigOptions>) => FlatConfigItem[];

export { defaultConfig };
